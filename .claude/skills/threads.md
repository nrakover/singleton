---
description: Show thread status board
---

Call `list_threads()` and `list_pending_approvals()` and render a status board showing all threads with their status, any pending approvals highlighted as needing attention.
